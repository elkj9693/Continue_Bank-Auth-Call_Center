server:
  port: 8082

spring:
  application:
    name: callcenter-was
  datasource:
    url: jdbc:mysql://localhost:3308/callcenter_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&characterEncoding=UTF-8
    username: root
    password: continue12!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

# Issuer (Bank) API Configuration
issuer:
  api:
    url: http://localhost:8085
  service-token: callcenter-service-token-2026

# ARS Service Token (for incoming ARS requests)
ars:
  service-token: local-dev-token

logging:
  level:
    com.callcenter.callcenterwas: DEBUG
    org.springframework.web: INFO
